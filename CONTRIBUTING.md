# Contributing to GuidelineOps-CN

感谢你为 GuidelineOps-CN 提交改进。本项目面向指南/共识元数据管理、证据追溯和医学审核流程；它不是临床诊断或处方系统。

## 开发环境

项目使用 Python 3.11–3.13 和 [uv](https://docs.astral.sh/uv/)。克隆仓库后运行：

```bash
uv sync
uv run ruff check .
uv run pytest -q
```

提交前还应运行 `uv lock --check` 和 `git diff --check`。修改数据模型或 CLI 时，请同时补充回归测试；保持输出稳定、可追溯，并避免把真实患者信息写入仓库、fixture 或日志。

## 医学内容与来源

- 新增来源适配器必须说明公开接口、许可/使用限制、抓取频率和失败重试策略。
- 不要把受版权保护的指南全文或未授权数据库镜像提交到仓库；只提交必要的元数据、短摘录和来源链接。
- 医学结论、用药规则和审核状态需要明确证据来源及人工复核边界。软件输出不能替代执业医师判断。
- 审核人员白名单是运行授权配置；不得在仓库中提交密码、证件号码或不必要的个人信息，也不得将其描述为执业资格验证。
- 涉及中医、西医或基层用药安全的变更，应在 PR 描述中列出适用人群、已知禁忌和待人工确认事项。

## Pull request 清单

1. 说明问题、设计取舍和可能的医学/数据风险。
2. 提供测试或可复现步骤，并确认 CI 通过。
3. 更新相关文档、数据字典或变更记录。
4. 确认没有提交凭据、个人信息、患者数据或不具备再分发许可的原始文档。

小而聚焦的 PR 更容易进行医学和工程双重审核。维护者可能要求医学专家复核涉及诊疗逻辑的变更。
